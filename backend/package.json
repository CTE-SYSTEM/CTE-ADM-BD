{
  "name": "backend",
  "version": "1.0.0",
  "description": "Sistema de Gestión para Centro Técnico Electrónico (CTE)",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "// DATABASE": "Comandos para gestión de base de datos",
    "db:sync": "prisma db push",
    "db:generate": "prisma generate",
    "studio": "prisma studio --port 5555",
    "db:seed": "docker exec -i postgres_cte psql -U User_admin -d Centro_Tecnico_Electronico < scripts/Seed_data.sql",
    "db:functions": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/load_functions.ps1",
    "db:seed:container": "psql \"$SQL_DATABASE_URL\" -f scripts/Seed_data.sql",
    "db:functions:container": "psql \"$SQL_DATABASE_URL\" -f scripts/load_functions.sql",
    "db:seed:local": "psql -U User_admin -d Centro_Tecnico_Electronico -f scripts/Seed_data.sql",
    "db:check:users": "docker exec postgres_cte psql -U User_admin -d Centro_Tecnico_Electronico -c \"SELECT id_usuario, nombre_usuario, rol, activo FROM \\\"Usuarios\\\" ORDER BY id_usuario;\"",
    "db:check:clientes": "docker exec postgres_cte psql -U User_admin -d Centro_Tecnico_Electronico -c \"SELECT * FROM \\\"Clientes\\\";\"",
    "db:check:tecnicos": "docker exec postgres_cte psql -U User_admin -d Centro_Tecnico_Electronico -c \"SELECT * FROM \\\"Tecnicos\\\";\""
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "prisma": "^5.22.0"
  },
  "keywords": [
    "express",
    "prisma",
    "postgresql",
    "backend",
    "electronic-service"
  ],
  "author": "CTE Dev Team",
  "license": "ISC"
}
